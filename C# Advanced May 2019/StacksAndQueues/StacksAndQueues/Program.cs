using System;

namespace StacksAndQueues
{
    class Program
    {
        static void Main(string[] args)
        {
            //Stack<T> (LIFO – last in, first out)
            //Push() Pushing an element at the top of the stack
            //Pop() Popping element from the top of the stack
            //Peek() Getting the topmost element without removing it
            //ToArray(), Contains() , Count

            //int count = stack.Count;
            //bool exists = stack.Contains(2);
            //int[] array = stack.ToArray();
            //stack.Clear();
            //stack.TrimExcess();


            //Queue<T> FIFo - first  in, first out

            //Enqueue() – Adds an element to the front
            //Dequeue() – Returns and removes the first element
            //Peek() – Returns the first element

            //Queue<int> queue = new Queue<int>();
            //int count = queue.Count;
            //bool exists = queue.Contains(2);
            //int[] array = queue.ToArray();
            //queue.Clear();
            //queue.TrimExcess();
        }
    }
}
